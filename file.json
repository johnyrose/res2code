[
    {
      "file": "res2code/apply_changes.py",
      "changes": [
        {
          "start_line": 4,
          "end_line": 9,
          "action": "replace",
          "old_code": "def apply_replace(file_content, start_line, end_line, old_code, new_code):\n    lines = file_content.splitlines()\n    new_lines = new_code.strip().splitlines()\n    lines[start_line - 1:end_line] = new_lines\n    return \"\\n\".join(lines) + \"\\n\"",
          "new_code": "def apply_replace(file_content, start_line, end_line, old_code, new_code):\n    lines = file_content.splitlines(True)  # Keep line endings\n    new_lines = new_code.splitlines(True)  # Keep line endings\n    lines[start_line - 1:end_line] = new_lines\n    return \"\".join(lines)"
        }
      ]
    }
  ]